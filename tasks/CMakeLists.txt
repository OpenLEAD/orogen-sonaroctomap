# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonaroctomapTaskLib)
ADD_LIBRARY(${SONAROCTOMAP_TASKLIB_NAME} SHARED 
    ${SONAROCTOMAP_TASKLIB_SOURCES})
add_dependencies(${SONAROCTOMAP_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SONAROCTOMAP_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SONAROCTOMAP_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONAROCTOMAP_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONAROCTOMAP_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SONAROCTOMAP_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONAROCTOMAP_TASKLIB_HEADERS}
    DESTINATION include/orocos/sonaroctomap)

